#include<stdio.h>
#include<conio.h>
int main()
{
	int num;
	printf("enter a number:");
	scanf("%d",&num);
	if(num%2==0)
	printf("the given no. is even:");
	else
	printf("the given no. is odd:");
}